# 任务文档目录

每个任务在 `docs/tasks/<slug>/` 下保存：

- `design.md`（系统架构师）：变更层归属、接口签名、数据模型、安全影响分析。
- `test-plan.md`（系统架构师）：要新增的单元/集成测试用例（含负面安全用例）。
- `test-report.md`（测试）：AC 覆盖率、门槛结果、缺陷分级。
- `review.md`（PR 审核员）：审核清单逐项结果与结论。
